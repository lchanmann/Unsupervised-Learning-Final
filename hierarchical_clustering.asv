window_size  = 30;
norm = 1; %manhattan
samples = MFCCs(1:15)';

tic;
dist_mtx = pdist(samples,dtw.new(window_size,norm));

cluster_tree = linkage(dist_mtx, 's');
t=toc;
fprintf('clustering complete after %d h, %d min, %f sec\n',floor(t/60^2),floor(t/60),rem(t,60));

dendrogram(cluster_tree);
